<h1>All Surgeries</h1>

<% @surgeries.each do |surgery| %>
  <section id ="surgery-<%= surgery.id %>">
    <h2> <%= link_to surgery.title, "surgeries/#{surgery.id}" %></h2>
    <p> In Room: <%= surgery.operating_room_number%></p>
  </section>
<% end %>
